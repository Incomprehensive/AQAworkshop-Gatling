#####################################
# Environment for test configuration#
#####################################
conf {
  # front urls
  env {
    mainUrl = "https://www.templatemonster.com"
  }
  # loading (users and duration)
  loading {
    usersCount = 500
    duration = 10
  }
}