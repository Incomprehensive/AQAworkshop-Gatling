#####################################
# Environment for test configuration#
#####################################
conf {
  # front urls
  dev {
    mainUrl = "****"
    cartsUrl = "****"
  }
  # loading (users and duration)
  loading {
    usersCount = 5
    duration = 5
  }
}