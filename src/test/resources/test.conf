#####################################
# Environment for test configuration#
#####################################
conf {
  # front urls
  dev {
    mainUrl = "https://www.templatemonsterdev.com/"
    cartsUrl = "https://service-carts.templatemonsterdev.com/api/"
  }
  # loading (users and duration)
  loading {
    usersCount = 5
    duration = 5
  }
}